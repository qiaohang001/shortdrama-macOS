name: Build macOS (烬序影墟)

# 触发方式：
#  1) 手动：GitHub 仓库 Actions 页面 → 点 "Run workflow"
#  2) 打 tag：git tag v0.3.0 && git push origin v0.3.0
on:
  workflow_dispatch:
  push:
    tags: ['v*']

jobs:
  build-macos:
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install frontend dependencies
        run: npm ci || npm install

      - name: Build frontend (dist)
        run: npm run build

      # macOS 需要自带 ffmpeg（Windows 用的 ffmpeg.exe 不能在 mac 上运行）
      - name: Bundle macOS ffmpeg
        run: |
          brew install ffmpeg
          mkdir -p src-tauri/resources
          cp "$(which ffmpeg)" src-tauri/resources/ffmpeg

      # 打包前把 tauri.conf.json 的 resources 从 ffmpeg.exe 换成 mac 版 ffmpeg
      - name: Patch tauri.conf.json resources for macOS
        run: |
          python3 - <<'EOF'
          import json, io
          p = 'src-tauri/tauri.conf.json'
          with io.open(p, encoding='utf-8') as f:
              cfg = json.load(f)
          res = cfg.get('bundle', {}).get('resources', {})
          if isinstance(res, dict):
              res.pop('resources/ffmpeg.exe', None)
              res['resources/ffmpeg'] = 'ffmpeg'
              cfg['bundle']['resources'] = res
          with io.open(p, 'w', encoding='utf-8') as f:
              json.dump(cfg, f, ensure_ascii=False, indent=2)
          print('patched resources ->', json.dumps(cfg['bundle']['resources'], ensure_ascii=False))
          EOF

      # macOS 打包 .dmg 必须要有 .icns 图标；没有则从 png 自动生成
      - name: Ensure .icns icon
        run: |
          if ls src-tauri/icons/*.icns >/dev/null 2>&1; then
            echo "icns already exists"
          else
            SRC=""
            for c in src-tauri/icons/app-icon.png src-tauri/icons/512x512.png src-tauri/icons/128x128.png src-tauri/icons/icon.png; do
              [ -f "$c" ] && SRC="$c" && break
            done
            if [ -z "$SRC" ]; then
              echo "::error::未找到图标源图，请确认 src-tauri/icons/ 目录下存在 png 图标"
              exit 1
            fi
            npx @tauri-apps/cli icon "$SRC" --output src-tauri/icons
          fi

      # 打 .dmg 安装包；产物同时上传到 GitHub Release
      - name: Build macOS dmg
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: --bundles dmg
          tagName: macos-v__VERSION__
          releaseName: '烬序影墟 macOS v__VERSION__'
          releaseBody: 'macOS 版安装包（.dmg）'
          releaseDraft: true
          prerelease: false

      # 额外把 dmg 作为 Actions artifact 保留（不依赖 Release）
      - name: Upload dmg artifact
        uses: actions/upload-artifact@v4
        with:
          name: 烬序影墟-macos-dmg
          path: |
            src-tauri/target/release/bundle/dmg/*.dmg
          if-no-files-found: error
